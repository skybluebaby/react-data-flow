import React from 'react';

const ALL_TODOS = 'all';
const ACTIVE_TODOS = 'active';
const COMPLETED_TODOS = 'completed';

const ENTER_KEY = 13;
const ESCAPE_KEY = 27;

const TEMP_STYLE: React.CSSProperties = {
  height: '100px',
  textAlign: 'center',
  fontSize: '40px',
  lineHeight: '100px',
  color: 'skyblue',
};

export {
  ALL_TODOS,
  ACTIVE_TODOS,
  COMPLETED_TODOS,
  ENTER_KEY,
  ESCAPE_KEY,
  TEMP_STYLE,
};
